"use strict";
// function that takes an object:
function displayPersonalInfo(obj) {
    return { obj };
}
let myObject = {
    name: "daniel",
    data: 15000
};
let output2 = displayPersonalInfo(myObject);
// use it use it:
let dansInfo = {
    name: "danny",
    data: 100 // have to use a number here coz thats what the type wants
};
